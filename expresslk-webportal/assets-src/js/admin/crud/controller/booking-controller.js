expressAdminCrudApp.controller('bookingController', ['$scope', '$window', function($scope, $window) {

        $scope.addPayment = function(bookingId) {
            //return false;
        }

        $scope.addRefund = function(bookingId) {
            //return false;
        }

        $scope.cancelBooking = function(bookingId) {
            return false;
        }

    }]);
